
  # High-Fidelity Mobile UI Design

  This is a code bundle for High-Fidelity Mobile UI Design. The original project is available at https://www.figma.com/design/LvVGQ1Qh7OFov6TZdsksms/High-Fidelity-Mobile-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  